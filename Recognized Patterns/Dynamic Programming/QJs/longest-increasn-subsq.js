log = (val) => console.log(val)
/**
 * Given an array of n elements, find the longest increasing subsequence
 * This is a maximum-length sequence of array elements that goes from 
 * left to right, and each element in the sequence is larger than the 
 * previous element. For example,
 * 
 * [6,2,5,1,7,4,8,3] => [2,5,1,7,8] => 5
 * 
 */

/**
 * Let length(k) = length of longest sequence up till index k
 * 
 * base case
 * if(arr.length == 1) return 1
 * f(arr.length == 0) return 0
 */
function solve(array) {
    
}
// log(coins.filter((c,i) => i > 0))

log(solve([6,2,5,1,7,4,8,3]))