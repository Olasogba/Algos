/**
 * Given string str, the task is to find the length of the longest substring
 * of str such that no three consecutive characters in the substring are 
 * same.

    Examples:

    Input: str = “baaabbabbb”
    Output: 7
    “aabbabb” is the required substring.

    Input: str = “babba”
    Output: 5
    Given string itself is the longest substring.
 */
log = (val) => console.log(val)

function longest(string) {
    
}

log(longest('aaabbabbb'))
log(longest('babbba'))
log(longest('abcaaabcbb'))